"""The cohort page of NGS-DOSE-1000G: `python -m report` builds docs/ from the counts files, on top of the
`ngsdose` library (https://github.com/jlanej/NGS-DOSE). `report.evidence_figure` and `report.trio_report` draw
the figure and the PDF from what it writes."""
